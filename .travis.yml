language: node_js
node_js:
  - "6"
  - "8"
  - "lts/*"

dist: trusty
sudo: false
cache: yarn
script:
  - yarn run tslint
  - yarn run test --coverage && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js
